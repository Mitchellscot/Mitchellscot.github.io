import Layout from "../../components/Layout/Layout"

export default function ContactLayout({ children, }: { children: React.ReactNode }) {
    return <Layout path={'contact'}>{children}</Layout>
}